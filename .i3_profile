#! /bin/bash

export BROWSER="firefox"
export BROWSER2="google-chrome-stable"

# Start graphical server if i3 not already running.
echo been here
pgrep -x i3 > /dev/null || exec startx
#if [ pgrep -x "i3" -gt /dev/null ]; then 
#	echo and here
	#exec startx
#fi
